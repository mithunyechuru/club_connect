/**
 * Admin Setup Utility
 * Run this once to set up the admin user in Firestore.
 * This file is a standalone Node.js script — NOT imported by the app.
 * 
 * Usage: node --experimental-vm-modules scripts/setupAdmin.mjs
 * 
 * But since we're in-browser, this logic is baked into the AdminSetupPage.
 */
